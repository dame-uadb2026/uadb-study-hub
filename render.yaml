services:
  - type: web
    name: uadb-study-hub
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: UADB_SECRET_KEY
        generateValue: true
